version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g patch-package
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .output
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
